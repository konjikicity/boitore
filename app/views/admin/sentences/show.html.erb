<div class="container">
  <div class="row">
    <div class="col-md-10 offset-md-1 col-lg-8 offset-lg-2">
      <h1><%= t('.title') %></h1>
      <div class="text-right mb-3">
        <%= link_to t('defaults.edit'), edit_admin_sentence_path(@mode), class: 'btn btn-success' %>
        <%= link_to t('defaults.delete'), admin_sentence_path(@mode), method: :delete, data: { confirm: t('defaults.message.delete_confirm') }, class: 'btn btn-danger' %>
      </div>
      <table class="table table-bordered bg-white">
        <tr>
          <th scope="row"><%= Mode.human_attribute_name(:id) %></th>
          <td><%= @sentence.id %></td>
        </tr>
        <tr>
          <th scope="row"><%= Mode.human_attribute_name(:title) %></th>
          <td><%= @sentence.normal %></td>
        </tr>
        <tr>
          <th scope="row"><%= Mode.human_attribute_name(:user) %></th>
          <td><%= @sentence.boin %></td>
        </tr>
        <tr>
          <th scope="row"><%= Mode.human_attribute_name(:created_at) %></th>
          <td><%= l @sentence.created_at, format: :long %></td>
        </tr>
      </table>
    </div>
  </div>
</div>